# Configuration pour exécution LOCALE sur le serveur Proxmox
# Copiez ce fichier en .env.local sur le serveur Proxmox

# Node Proxmox (utiliser 'pvenode status' pour voir le nom)
TARGET_NODE=proxmox

# Template LXC Debian (déjà présent sur Proxmox)
TEMPLATE_NAME=debian-12-standard_12.12-1_amd64.tar.zst

# Stockage pour les containers
VM_STORAGE=local-lvm

# Clé SSH publique (ajoutez votre clé publique ici)
SSH_KEYS="ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIOImjt93r+hfs94VG8ytJnaV3PPb4i/kIoMUYQS+T81O terraform-gsb"

# Credentials par défaut des containers (Cloud-init)
CI_USER=sio2027
CI_PASSWORD=Formation13@

# Authentification Proxmox (pour exécution locale avec mot de passe)
PM_USER=root@pam
PM_PASSWORD=votre-mot-de-passe-root
